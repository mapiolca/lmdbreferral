# ChangeLog

## Unreleased

- Mise en conformité de la numérotation des permissions sur le format `{ID module} * 100 + $r`, avec migration idempotente des anciennes attributions `450024` à `450032` vers `45002301` à `45002309`.

## 1.0.0

- Création du module `lmdbreferral` avec ID Dolibarr `450023`.
